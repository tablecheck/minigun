# Testing
- Do not jury-rig tests
- Avoid using mocks in tests
- Whenever you create a file in /examples dir, always run `git update-index --chmod=+x <file_path>` to ensure it have execute permissions.
